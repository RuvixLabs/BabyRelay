{
  "name": "babyrelay-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "lint": "node --check index.js && node --check lib/sleepLiveActivity.js && node --check lib/tokenCleanup.js && node --check lib/superwallEntitlement.js",
    "test": "node --test test/*.test.js",
    "test:rules": "firebase emulators:exec --config ../firebase.json --project demo-babyrelay --only firestore \"node --test test/firestoreRules.rules.js\""
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "google-auth-library": "^10.5.0",
    "svix": "^1.81.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
